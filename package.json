{
  "name": "stock-market-info",
  "version": "1.0.0",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/gregdardis/stock-market-info.git"
  },
  "license": "ISC",
  "scripts": {
    "build": "webpack --config ./src/client/config/webpack.config.js",
    "clean": "rm -r node_modules && cd src/client/ && rm -r node_modules && cd ../../",
    "dev": "webpack-dev-server --config ./src/client/config/webpack.config.js",
    "dev:test": "npm-run-all --parallel dev test:watch",
    "postinstall": "cd src/client/ && npm install && cd ../../",
    "start": "nodemon src/server/server.js",
    "test": "mocha --compilers js:babel-core/register",
    "test:watch": "mocha --watch --compilers js:babel-core/register"
  },
  "devDependencies": {
    "eslint": "^4.14.0",
    "eslint-plugin-mocha": "^4.11.0",
    "mocha": "^4.1.0",
    "nodemon": "^1.14.6",
    "npm-run-all": "^4.1.2"
  },
  "dependencies": {
    "chai": "^4.1.2",
    "express": "^4.16.2",
    "yahoo-finance": "^0.3.5"
  }
}
